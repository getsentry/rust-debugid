language: rust
cache:
  cargo: true

git:
  depth: 1

if: tag IS blank
script: make $SUITE

matrix:
  include:
    - os: linux
      env: SUITE=test
    - os: linux
      env: SUITE=test
      rust: "1.19.0"
    - os: linux
      env: SUITE=format-check
      install: rustup component add rustfmt-preview
